import {TxUrlDbPortal, TxDashboard} from '/snippets/text/arcana.mdx';

1. Register the app and get a unique **client ID** via the {TxDashboard} <TxUrlDbPortal/>

2. Optionally, configure social login providers and gasless transactions for the app via the dashboard.